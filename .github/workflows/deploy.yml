name: Deploy To Github Pages

on:
  push:
    branches:
      - "master"

permissions:
  contents: read

jobs:
  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          node-version: 16.10.0
      - run: |
          npm install
          npm run deploy